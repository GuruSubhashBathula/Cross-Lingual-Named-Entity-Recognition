name: cross-lingual-ner
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - ipykernel
  - jupyter
  - numpy>=1.24.0
  - pandas>=2.0.0
  - matplotlib>=3.7.0
  - pip:
      - torch>=2.0.0
      - transformers>=4.40.0
      - datasets>=2.19.0
      - seqeval>=1.2.2
      - accelerate>=0.30.0
      - evaluate>=0.4.2